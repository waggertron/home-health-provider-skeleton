[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["migrations", "node_modules", ".venv", "**/migrations/**"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "DJ"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["hhps", "tenancy", "accounts", "core", "seed"]
